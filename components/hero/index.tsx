import React from "react";
import Carousel from "../carousel";

function Hero() {
  return (
    <section>
      <Carousel />
      <div>
        <button>left</button>
        <button>left</button>
      </div>
    </section>
  );
}

export default Hero;
